Tool for exporting Musiclibrary XML file to JDBC database (Derby, HyperSQL), MongoDB, ActiveMQ, and JSON file, 
and importing from database (Derby, HyperSQL) and MongoDB to Musiclibrary XML.

Run MusicLibraryToolsSpring -h for options. 